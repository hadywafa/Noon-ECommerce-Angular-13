import { AfterDiscountPricePipe } from './after-discount-price.pipe';

describe('AfterDiscountPricePipe', () => {
  it('create an instance', () => {
    const pipe = new AfterDiscountPricePipe();
    expect(pipe).toBeTruthy();
  });
});
